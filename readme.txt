This code written in Javascript, CSS and HTML displays a loading screen whilst waiting for images to load to the site. 

It waits for the animation to complete a cycle before fading out and the content fades in. 

If an image loads or fails to load, it adds to the count of images. When all images are loaded. The loader fades out and the page fades in. 